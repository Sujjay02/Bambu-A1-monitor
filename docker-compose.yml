services:
  monitor:
    build: .
    network_mode: host
    env_file: .env
    volumes:
      - ./print_telemetry.csv:/app/print_telemetry.csv
      - ./failure_snapshots:/app/failure_snapshots
    restart: unless-stopped
